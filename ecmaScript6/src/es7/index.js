// Include
// Trabaja sobre un arreglo o un string nos permite saber si hay un elemento dentro de este valor.

let numbers = [1, 2, 3, 7, 8];
if(numbers.includes(7)){
    console.log('Si se encuentra');
}else{
    console.log('No encuentra');
}

// Elevar a la potencia.
let base = 4;
let exponent = 3;
let result = base ** exponent;

console.log(result);